[project]
name = "da-motion-sticker-skill"
version = "1.0.0"
description = "Codex skill helpers for nine-grid transparent animated sticker packs"
requires-python = ">=3.9"
dependencies = ["Pillow>=9.0", "numpy>=1.21"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
